# Git LFS tracking untuk file besar
# Model ONNX 175MB - harus pakai LFS agar bisa masuk ke Git
trained_models/*.onnx filter=lfs diff=lfs merge=lfs -text
trained_models/*.pth filter=lfs diff=lfs merge=lfs -text
trained_models/*.pt filter=lfs diff=lfs merge=lfs -text

# Data files besar
data/**/*.tif filter=lfs diff=lfs merge=lfs -text
data/**/*.nc filter=lfs diff=lfs merge=lfs -text
